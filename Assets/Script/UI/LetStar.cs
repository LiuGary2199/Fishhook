using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LetStar : MonoBehaviour
{
[UnityEngine.Serialization.FormerlySerializedAs("TrailEffect")]    //public ParticleSystem ShowEffect;
    public ParticleSystem ToughMisery;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
